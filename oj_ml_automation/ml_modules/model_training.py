"""
Cross Validation and Train/test set predictions and metrics


add feat_ to all columns
"""


def train_model(train_dataset, target_variable_column, run_directory):
    """K-Fold cross validation
    model = sklearn.classifier.fit()
    model.save(run_directory/model_binaries/model_random_id.pickle)
    TODO: Implement code below
    """
    return None